#include <stdio.h>

int main()
{
  int a, b, c;
  printf("print a, b, c as numbers\n");
  scanf("%d %d %d", &a, &b, &c);

        int d = a;
        if (b > c && b > a) d = b;
        if (c > a && c > b) d = c;

   printf("%d is Max number", d );


  return 0;

}
